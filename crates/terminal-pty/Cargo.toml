[package]
name = "terminal-pty"
version = "0.0.2"
edition = "2024"
license = "MIT OR Apache-2.0"

[dependencies]
portable-pty = "0.9"
metrics = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }

[lints]
workspace = true
