[package]
name = "terminal-screen"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[dependencies]
unicode-width = "0.2"
metrics = { workspace = true }

[dev-dependencies]
criterion = "0.5"
proptest = "1"

[[bench]]
name = "screen_bench"
harness = false

[lints]
workspace = true
