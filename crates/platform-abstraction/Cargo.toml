[package]
name = "platform-abstraction"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[dependencies]
winit = { workspace = true }
arboard = "3"
tracing = { workspace = true }

[lints]
workspace = true
