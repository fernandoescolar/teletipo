[package]
name = "ui"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[dependencies]
platform-abstraction = { path = "../platform-abstraction" }
winit = { workspace = true }

[lints]
workspace = true
