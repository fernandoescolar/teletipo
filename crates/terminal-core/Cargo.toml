[package]
name = "terminal-core"
version = "0.1.0"
edition = "2024"

[dependencies]
terminal-ansi = { path = "../terminal-ansi" }
terminal-screen = { path = "../terminal-screen" }
thiserror = "2"

[dev-dependencies]
criterion = "0.5"

[lints]
workspace = true
